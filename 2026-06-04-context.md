# Dindin — Session Context  2026-06-04

## Project
Expo / React Native app (SDK 56, managed workflow).  
Bundle ID: `in.istudio.dindin` · Supabase backend · Claude Haiku for AI features.

---

## What was built today

### Bug fixes
| Issue | Fix |
|---|---|
| `expo-file-system` `readAsStringAsync` deprecated | Migrated to `new File(uri).base64()` from `expo-file-system` |
| Fridge scan "no couple linked" in solo mode | DB migration: `couple_id` nullable in `inventory_items`; new RLS policy allows solo access via `added_by_user_id` |
| Fridge scan second photo HEIC → Claude 400 error | `expo-image-manipulator` converts picker images to JPEG before sending |
| Progress page "text strings must be rendered within a Text component" | Replaced multi-line JSX text with template literal in goal section |
| `ExpoWebBrowser` native module crash | Removed top-level import; Google sign-in stubbed out until dev build ready |
| Supabase realtime "cannot add callbacks after subscribe()" | `useInventory` subscription uses `fetchItemsRef` to break `fetchItems` dependency; `MealPlanSection` no longer calls `useInventory` internally — inventory passed as prop |

### Features added
1. **Fridge scan improvements**
   - Inline editing: name/qty/unit fields with ✓ / ✕ buttons, keyboard chaining
   - Multi-photo scanning: horizontal photo strip, "+ Scan more" button, JPEG conversion
   
2. **Progress screen redesign** (Headspace-style)
   - Calorie ring hero card
   - 2-column quick-action tiles (⚖️ Weight · ⚡ Activity) — each tap opens log sheet
   - Compact horizontal macro bars (🥩 🍞 🫙)
   - Animated streak pill in header
   - Weight trend chart + milestones below

3. **Manual workout tracking**
   - `useHealthData` hook — upserts to `health_data` table with `source='manual'`
   - `WorkoutLogSheet` — activity chips, big duration input, MET-based calorie estimate
   - Calories burned feed into CalorieRing

4. **3-Day AI Meal Plan** (home screen hero feature)
   - `mealPlanner.ts` — Claude Haiku prompt with profile + fridge + saved recipes
   - `useMealPlan` hook — AsyncStorage cache keyed by `userId_date_cuisine`; `clearMealPlanCache` exported
   - `MealPlanSection` — day tabs, cuisine chips (10 options), animated meal cards
   - Cards: tap to expand have/missing ingredient chips
   - ❤️ heart to like (persisted to `liked_meals_v1` in AsyncStorage)
   - ↺ swipe-left or tap button to replace individual meal (calls `refreshSingleMeal`)
   - Skeleton loading + error/retry state

5. **Cuisine selector**
   - 10 options: Any · Indian · Italian · Asian · Mexican · Mediterranean · Japanese · Thai · Middle Eastern · American
   - Persisted in AsyncStorage; each cuisine+date has its own cache slot

6. **Outside food nudge**
   - After logging restaurant/delivery meal → amber nudge card with calorie count + healthy tip
   - Time-based "🍕 Ordering in or eating out?" prompt during meal times (12–14h, 18–21h)

7. **Dietary preference → meal plan sync**
   - `SettingsScreen.handleSave` calls `clearMealPlanCache` after save
   - Next home screen open regenerates plan with updated restrictions

8. **Profile & Settings page**
   - Avatar button (top-right home screen) → `SettingsScreen` modal
   - Profile card shows: Google photo or initials, name, email, provider badge (G / ✉)
   - Prominent "Sign out" button at top
   - Home screen avatar shows Google profile photo if signed in with Google

9. **Google Sign-In (UI ready, stubbed)**
   - "G Continue with Google" button on AuthScreen
   - Taps shows "Coming soon — requires native build" alert
   - Will be enabled after `eas build --profile development` + Supabase Google provider config

---

## Supabase migrations run today

### `001_solo_inventory.sql`
```sql
ALTER TABLE public.inventory_items ALTER COLUMN couple_id DROP NOT NULL;

DROP POLICY IF EXISTS "inventory_couple_access" ON public.inventory_items;

CREATE POLICY "inventory_access" ON public.inventory_items FOR ALL
  USING (
    (couple_id IS NOT NULL AND couple_id = public.my_couple_id())
    OR
    (couple_id IS NULL AND added_by_user_id = auth.uid())
  );
```

### Profile fix (run manually)
```sql
INSERT INTO public.profiles (id, display_name)
SELECT id, COALESCE(raw_user_meta_data->>'display_name', email, '')
FROM auth.users
WHERE id NOT IN (SELECT id FROM public.profiles);
```

---

## Key file map
| File | Purpose |
|---|---|
| `src/services/mealPlanner.ts` | Claude API — generate 3-day plan + single-meal refresh |
| `src/hooks/useMealPlan.ts` | Meal plan state, AsyncStorage cache, cuisine preference |
| `src/hooks/useHealthData.ts` | Manual workout logging, MET calorie calc |
| `src/hooks/useInventory.ts` | Fridge inventory — handles solo (null coupleId) and couple modes |
| `src/components/home/MealPlanSection.tsx` | Full meal plan UI — cuisine chips, day tabs, swipe/heart cards |
| `src/components/progress/WorkoutLogSheet.tsx` | Workout log bottom sheet |
| `src/screens/ProgressScreen.tsx` | Redesigned progress dashboard |
| `src/screens/FridgeConfirmScreen.tsx` | Multi-photo fridge scan with JPEG conversion |
| `src/components/camera/IngredientsList.tsx` | Inline ingredient editing with keyboard chaining |
| `src/context/AuthContext.tsx` | Auth — email/password + Google stub |

---

## Google OAuth — pending setup
1. Google Cloud Console → create iOS OAuth client, bundle ID `in.istudio.dindin`
2. Supabase → Authentication → Providers → Google → paste client ID + secret
3. Add redirect URL `dindin://auth/callback`
4. Run `eas build --profile development --platform ios`
5. Restore `signInWithGoogle` implementation in `AuthContext.tsx` (see chat history)

---

## Known / deferred
- Waste log feature requires couple (solo mode not yet supported for waste)
- Inventory screen (`InventoryScreen`) imported in `App.tsx` but not rendered in any tab — dead import
- `FridgeConfirmScreen` footer hint still says "Long-press to edit" — should say "Tap qty to edit"
